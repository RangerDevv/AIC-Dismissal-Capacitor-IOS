import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "co.median.android.mrbaqx",
  appName: "SimpleDismiss",
  webDir: "build",
  bundledWebRuntime: false,
};

export default config;
