import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.aicdismissal.vercel',
  appName: 'SimpleDismiss',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;